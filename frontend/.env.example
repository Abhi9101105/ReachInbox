# ReachInbox Frontend Environment Variables (Optional)
# In development, Vite proxy routes /api and /admin/queues to http://localhost:4000.
# In production, set VITE_API_URL if the backend is hosted on a separate domain.
# VITE_API_URL=https://api.yourdomain.com/api
